# Android SMS Gateway connection (via ADB USB forward)
ANDROID_GATEWAY_URL=http://localhost:8081
ANDROID_GATEWAY_USER=admin
ANDROID_GATEWAY_PASS=secret
ANDROID_GATEWAY_PORT=8081

# SMS Gate API server
API_HOST=0.0.0.0
API_PORT=8080
API_KEY=change-me-to-random-string
